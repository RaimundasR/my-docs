title: My Docs Site
remote_theme: jekyll/minima
plugins:
  - jekyll-remote-theme

markdown: kramdown

collections:
  docs:
    output: true
    permalink: /:collection/:name/

defaults:
  - scope:
      path: ""
      type: docs
    values:
      layout: default
